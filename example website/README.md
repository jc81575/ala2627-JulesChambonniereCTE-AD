# Champions League motion lab

Open `index.html` in a browser. This is a flexbox and CSS motion demo with six Champions League-inspired club badge studies:

- ambient float
- perspective tilt
- hover shimmer
- orbiting detail
- 3D flip
- focus pulse

The page includes a `prefers-reduced-motion` fallback and works without a build step.
